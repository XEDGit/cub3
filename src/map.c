#include <shared.h>

bool	is_valid_map(char ch)
{
	if (ch == '0' || ch == '1' || ch == 'N' || ch == 'S' || ch == 'E' || ch == 'W')
		return (true);
	return (false);
}

bool	check_map(t_map *map)
{
	return (false);
}